package controller;

import javafx.fxml.FXML;
import javafx.fxml.Initializable;
import javafx.scene.control.Label; // Contoh jika ada elemen di FXML

import java.net.URL;
import java.util.ResourceBundle;

public class pendekatanPasarController implements Initializable {

    // @FXML
    // private Label exampleLabel; // Contoh jika ada elemen di FXML yang perlu diakses

    @Override
    public void initialize(URL url, ResourceBundle resourceBundle) {
        System.out.println("MarketApproachController initialized.");
        // Logika inisialisasi untuk halaman pendekatan pasar
        // exampleLabel.setText("Data Pendekatan Pasar Dimuat!");
    }

    // Metode lain untuk halaman ini
}